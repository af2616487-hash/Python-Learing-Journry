s = input().lower()
for ch in ".,!?;:":
    s = s.replace(ch, "")
words = s.split()

freq = {}
for w in words:
    freq[w] = freq.get(w, 0) + 1

for w, c in sorted(freq.items(), key=lambda x: (-x[1], x[0])):
    print(f"{w}:{c}")
